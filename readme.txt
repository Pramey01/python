Find the file named startup.sh
.sh is the file extension typically for a script
to start the script use this example
	$ ./startup.sh
Within the terminal type everything in front of the dollar sign
Type in your password then the script will run and not require any other user interaction. The terminal window can now be minimized

